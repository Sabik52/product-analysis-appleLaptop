import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h4>Q.No:1 What is Context API</h4>
            <p>Answer: Context API is Share state within a component tree quaite easily.In React apps we normally use props to share the data to the child components, but in some cases, we need to pass the same prop to many components in such cases context API shines  </p>

            <h4>Q.No:2 What is semantic Tag..?</h4>
            <p>Answer: A semantic element clearly describes its meaning to both the browser and the developer. Examples of semantic elements: form , table , and article- Clearly defines its content. </p>

        </div>
    );
};

export default Blogs;